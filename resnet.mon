ResNet{
	In -> Repeat(layer=ResNetBlock, size=100) -> Out

    ResNetBlock{
		In -> Double -> Summation -> Out

		Double{Identity | Block}
        Block{In -> Conv -> BatchNorm -> ReLU -> Conv -> BatchNorm -> Out}
    }
}